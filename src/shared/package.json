{
  "name": "@skill-gap-analyzer/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for Skill Gap Analyzer",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0",
    "@types/uuid": "^9.0.7"
  },
  "peerDependencies": {
    "typescript": ">=5.0.0"
  }
}